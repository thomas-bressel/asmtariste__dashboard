# name: Deploy to Production

# on:
#   workflow_run:
#     workflows: ["Projet Asmtariste - Dashboard CI"]
#     types:
#       - completed
#     branches:
#       - main

# jobs:
#   deploy-production:
#     runs-on: ubuntu-latest
#     if: github.event.workflow_run.conclusion == 'success'
    
#     steps:
#     - name: Download build artifacts
#       uses: actions/github-script@v7
#       with:
#         script: |
#           let allArtifacts = await github.rest.actions.listWorkflowRunArtifacts({
#              owner: context.repo.owner,
#              repo: context.repo.repo,
#              run_id: context.payload.workflow_run.id,
#           });
#           let matchArtifact = allArtifacts.data.artifacts.filter((artifact) => {
#             return artifact.name == "angular-build-main"
#           })[0];
#           let download = await github.rest.actions.downloadArtifact({
#              owner: context.repo.owner,
#              repo: context.repo.repo,
#              artifact_id: matchArtifact.id,
#              archive_format: 'zip',
#           });
#           let fs = require('fs');
#           fs.writeFileSync(`${process.env.GITHUB_WORKSPACE}/angular-build.zip`, Buffer.from(download.data));

#     - name: Extract artifacts
#       run: |
#         unzip angular-build.zip
#         ls -la
    
#     - name: Deploy to Production FTP Server
#       uses: SamKirkland/FTP-Deploy-Action@v4.3.4
#       with:
#         server: ${{ secrets.FTP_SERVER_PRODUCTION }}
#         username: ${{ secrets.FTP_USERNAME_PRODUCTION }}
#         password: ${{ secrets.FTP_PASSWORD_PRODUCTION }}
#         local-dir: ./
#         server-dir: /dashboard/
#         exclude: |
#           **/.git*
#           **/.git*/**
#           **/node_modules/**
#           angular-build.zip